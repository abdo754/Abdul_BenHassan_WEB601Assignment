import { FilterContentTypePipe } from './filter-content-type.pipe';

describe('FilterContentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterContentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
